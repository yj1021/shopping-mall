import React, { ReactElement } from 'react'

interface Props {
    
}

export default function StoreSetting({}: Props): ReactElement {
    return (
        <div>
            店铺设置
        </div>
    )
}
